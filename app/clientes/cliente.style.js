import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        marginBottom: 15
    },
    label: {
        fontSize: 20,
        alignSelf: 'center'
    }
})

export default styles;